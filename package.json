{
  "name": "sva-beast-mode-project",
  "version": "1.0.0",
  "description": "Cross-platform application with good software engineering practices.",
  "main": "main.js",
  "repository": "git@github.com:sergiovegasalonso/sva-beast-mode-project.git",
  "author": "<sergio.vegas.alonso@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "preview": "yarn --cwd src build && NODE_ENV=development electron-forge start",
    "package": "yarn --cwd src build && electron-forge package",
    "make": "yarn --cwd src build && electron-forge make"
  },
  "devDependencies": {
    "@electron-forge/cli": "^6.0.5",
    "@electron-forge/maker-deb": "^6.0.5",
    "@electron-forge/maker-rpm": "^6.0.5",
    "@electron-forge/maker-squirrel": "^6.0.5",
    "@electron-forge/maker-zip": "^6.0.5",
    "electron": "^23.0.0"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.0"
  }
}
